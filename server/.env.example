# Copy to .env and fill in. The server runs fine without these (STUN-only);
# set them to enable Cloudflare TURN relay for cross-network / cellular calls.
# Get both from the Cloudflare dashboard: Realtime > TURN > your TURN key.
TURN_KEY_ID=
TURN_API_TOKEN=
