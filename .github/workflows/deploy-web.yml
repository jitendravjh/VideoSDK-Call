name: Deploy Flutter Web

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: flutter-web-deploy
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - run: flutter pub get

      # base-href must match the GitHub Pages sub-path (project site = /<repo>/).
      # The web build connects to the public signalling server over wss
      # (AppConfig.fallbackUrl), so calls work from the browser across networks.
      - run: flutter build web --release --base-href "/${{ github.event.repository.name }}/"

      - uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: build/web
          clean: true
